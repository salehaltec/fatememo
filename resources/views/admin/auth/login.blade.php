<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ورود مدیریت</title>
<style>body{font-family:Tahoma;background:#f3f4f6;margin:0;display:grid;place-items:center;min-height:100vh}.box{background:#fff;padding:28px;border-radius:16px;width:min(380px,calc(100% - 32px));box-shadow:0 12px 35px #0001}input,button{box-sizing:border-box;width:100%;padding:12px;border-radius:9px;margin:7px 0;border:1px solid #d1d5db}button{background:#1e40af;color:#fff;border:0;font-weight:bold}.err{color:#b91c1c;font-size:13px}</style></head><body><div class="box"><h2>ورود مدیر</h2><p>کد ورود فقط به شماره مدیر ارسال می‌شود.</p>
@if(session('otp_sent'))<form method="post" action="{{ route('admin.login.verify') }}">@csrf<input type="hidden" name="phone" value="{{ old('phone') }}"><input name="code" inputmode="numeric" maxlength="6" placeholder="کد ۶ رقمی" autofocus><button>ورود به داشبورد</button></form>
@else<form method="post" action="{{ route('admin.login.send') }}">@csrf<input name="phone" value="{{ old('phone') }}" inputmode="tel" dir="ltr" placeholder="09xxxxxxxxx"><button>ارسال کد ورود</button></form>@endif
@foreach($errors->all() as $error)<div class="err">{{ $error }}</div>@endforeach</div></body></html>
